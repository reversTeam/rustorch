//! Element dtypes.
//!
//! Concrete enum + bit-width tables land in P1.1 (Tensor core). Placeholder
//! for now so the workspace compiles.

/// Placeholder marker for the dtype module — real impl in P1.1.
#[allow(dead_code)]
pub(crate) const _PLACEHOLDER: () = ();
